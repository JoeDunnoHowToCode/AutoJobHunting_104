/** Interface skeleton only — no cutting logic yet. */

export function countCjkChars(_value: string): number {
  return 0;
}

export function cutAtSentenceBoundary(_text: string, _maxCjkChars: number): string {
  return '';
}
