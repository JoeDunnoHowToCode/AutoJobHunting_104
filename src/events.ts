/** Interface skeleton only — no formatting logic yet. */
export type EventFields = Record<string, string | number | boolean | null | undefined>;

export function formatEvent(_name: string, _fields: EventFields): string {
  return '';
}
